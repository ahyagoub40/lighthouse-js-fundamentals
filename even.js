const isEven = function (num){  // function that takes a number 
    return num % 2 ===0;  // it returns if the numbers is divisible by 2 (true / false)
}

// call the function and display the value to the console 
console.log(isEven(10));       
console.log(isEven(11));
